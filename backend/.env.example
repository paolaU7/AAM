# Copy to .env and adjust. DATABASE_URL is required.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/aam
PORT=8000
# Secret for signing panel session tokens. Change for anything real.
JWT_SECRET=dev-insecure-change-me
