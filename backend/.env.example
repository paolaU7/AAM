# Database
DATABASE_URL=postgresql://aam_user:aam_password@localhost:5432/aam_dev

# FastAPI
DEBUG=True
# Replace this value in your local `.env` with a secure key (do NOT commit real keys)
API_KEY_DEVICE=REPLACE_WITH_SECURE_KEY

# Environment
ENVIRONMENT=development
